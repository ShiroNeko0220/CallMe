server:
  port: 8085

spring:
  application:
    name: presence-ms

  datasource:
    url: jdbc:mysql://localhost:3306/presence_db
    username: user
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true