server:
  port: 8083

spring:
  application:
    name: competition-ms

  datasource:
    url: jdbc:mysql://localhost:3306/competition_db
    username: user
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true