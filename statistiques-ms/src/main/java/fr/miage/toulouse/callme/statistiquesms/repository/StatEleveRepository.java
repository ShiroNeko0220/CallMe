package fr.miage.toulouse.callme.statistiquesms.repository;

import fr.miage.toulouse.callme.statistiquesms.entity.StatEleve;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StatEleveRepository extends JpaRepository<StatEleve, Long> {
}
