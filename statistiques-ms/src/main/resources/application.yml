server:
  port: 8086

spring:
  application:
    name: statistiques-ms

  datasource:
    url: jdbc:mysql://localhost:3306/statistiques_db
    username: user
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true